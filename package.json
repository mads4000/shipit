{
  "name": "guttimachine",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd frontend && npm run dist && cd ..",
    "dev": "npm run build && ./node_modules/.bin/nodemon index.js",
    "start": "node index.js"
  },
  "author": "ship-it-day",
  "license": "ISC",
  "dependencies": {
    "async": "^2.1.4",
    "nodemon": "^1.11.0",
    "restify": "^4.0.0"
  },
  "devDependencies": {
    "johnny-five": "^0.10.6",
    "raspi-io": "^7.1.0",
    "rpi-gpio": "^0.7.0"
  }
}
